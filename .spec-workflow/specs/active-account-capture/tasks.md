# Tasks Document — active-account-capture (S17)

> First-run UX: when the live active account isn't in the vault, name it + offer one-click capture (Configurations + an Overview nudge). Presentation-only — NO backend change; capture keeps the existing explicit `openAddAccount` path; secrets stay in Rust. Tokens-only styling. i18n in 5 locales (zh-Hans complete). Keep all existing tests green. Each task: set `[-]`, implement, `log-implementation`, then `[x]`.

- [x] 1. useActiveAccountCapture() hook + shared matcher + i18n keys
  - Files: `src/lib/queries.ts` (modify), `src/screens/configurations/index.tsx` (export/lift `accountIsActive` if needed), `src/i18n/locales/en.json` + `zh-Hans.json` + `zh-Hant.json` + `ja.json` + `fr.json` (modify)
  - Purpose: one shared source of `needsCapture` + the new copy
  - _Leverage: useActiveIdentity, useAccounts, the existing accountIsActive matcher_
  - _Requirements: 1.1, 1.2, 1.3_
  - _Prompt: Implement the task for spec active-account-capture, first run spec-workflow-guide to get the workflow guide then implement the task: Role: React data engineer | Task: Add useActiveAccountCapture() to src/lib/queries.ts returning ( needsCapture: boolean, email: string | null ): needsCapture = active identity kind === "account" AND no saved account matches it by email (reuse the existing accountIsActive(account, identity) matcher from configurations — export it, or factor a tiny shared emailEq so there is ONE matcher); false while either query loads and for provider/none kinds; email = the active identity email. Add the i18n keys to en.json + the 4 locales (zh-Hans complete): configs.capture.signedInAs ("You're signed in as ((email))" with interpolation), configs.capture.addToVault, configs.capture.uncaptured ("Add the account you're signed into"), overview.addThisAccount ("Add this account"). | Restrictions: ONE matcher (no duplicate email logic); no new backend; demo fallback already covered by the hooks. | Success: tsc clean; the hook returns the right needsCapture for account/provider/none/captured cases._

- [x] 2. Configurations: concrete empty-state + uncaptured-active banner
  - Files: `src/screens/configurations/index.tsx` (modify)
  - Purpose: name the detected account + one-click capture (both empty and partial states)
  - _Leverage: useActiveAccountCapture, openAddAccount (existing modal), @/ui Button/Card, lucide UserPlus/Plus_
  - _Requirements: 2.1, 2.2, 2.3, 2.4_
  - _Prompt: Implement the task for spec active-account-capture, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Frontend developer | Task: In src/screens/configurations/index.tsx use useActiveAccountCapture(). (a) AccountsEmptyState: when needsCapture + email, render the concrete copy t("configs.capture.signedInAs", ( email )) + the existing Add current account button (-> openAddAccount); else keep the generic copy. (b) When needsCapture AND accountList.length > 0, render a CaptureActiveRow as the FIRST item inside the accounts Card (accent-tinted left edge, a UserPlus icon, t("configs.capture.uncaptured") + the email, and a capture Button -> openAddAccount), above the saved rows; hide it when not needsCapture. Use useTranslation; tokens only; the capture path is the existing explicit modal (no new write). | Restrictions: no silent capture; presentation only; existing add/switch/remove rows unchanged; tokens only. | Success: empty+uncaptured names the email; existing-accounts+uncaptured shows the banner; captured shows neither; tsc clean._

- [x] 3. Overview tile nudge + tests + verify
  - Files: `src/screens/overview/index.tsx` (modify), `src/lib/queries.test.ts` (modify), `src/screens/configurations/Configurations.test.tsx` (new or modify)
  - Purpose: the light Overview nudge + lock the behavior + prove green
  - _Leverage: useActiveAccountCapture, go('configs')/openAddAccount, Vitest_
  - _Requirements: 3.1, all_
  - _Prompt: Implement the task for spec active-account-capture, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Frontend developer + QA | Task: In src/screens/overview/index.tsx, when useActiveAccountCapture().needsCapture, add a small muted "Add this account" affordance on the Claude accounts StatTile (or just below it) whose click does go("configs") then openAddAccount; unobtrusive, absent once captured. TESTS: queries.test.ts — useActiveAccountCapture true for account+empty (with email), false for captured / provider / none / loading. Configurations test — empty+uncaptured names the email + capture button calls openAddAccount; existing-accounts+uncaptured shows the banner; captured shows neither. VERIFY: pnpm exec tsc --noEmit (0) + pnpm test (green incl. new) + pnpm exec vite build (clean) + fingerprint grep over the touched files (zero). Report exact results. (The orchestrator screenshots the new prompt + commits.) | Restrictions: behavior not implementation in tests; tokens only; do not git commit. | Success: all gates green; the nudge shows/hides correctly._
