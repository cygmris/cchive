# Tasks Document — import-export-backups-latency (S15)

> cc-switch-inspired enhancements, secrets-safe. Export NEVER contains keys/tokens (tested). Backups snapshot/restore only Claude file CONTENT (never the keyring). Latency sends NO auth. Atomic writes, backups dir 0700 / files 0600. De-fingerprinted (no predecessor file names like ccconfig). Identity app.clavis. Each task: set `[-]`, implement, `log-implementation`, then `[x]`.

- [x] 1. core/portable.rs — secret-free export/import + dialog plugin + Rust tests
  - Files: `src-tauri/src/core/portable.rs` (new), `src-tauri/src/core/mod.rs` (modify), `src-tauri/src/model.rs` (modify), `src-tauri/src/commands/portable.rs` (new), `src-tauri/src/commands/mod.rs` (modify), `src-tauri/src/lib.rs` (modify), `src-tauri/Cargo.toml` (modify), `src-tauri/capabilities/default.json` (modify)
  - Purpose: portable export/import that never carries secrets
  - _Leverage: src-tauri/src/core/(providers,claude_json,atomic_fs).rs, S5 prefs/providers index_
  - _Requirements: 1.1, 1.2, 1.3, 1.4_
  - _Prompt: Implement the task for spec import-export-backups-latency, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Rust engineer | Task: Add tauri-plugin-dialog to Cargo.toml + register in lib.rs + grant the dialog open/save permissions in capabilities/default.json (narrow). Add ExportDoc (app "clavis", schema u32, exported_at, providers as (label,baseUrl,model) WITHOUT key, prefs, accounts as (label,meta) WITHOUT tokens) + ImportSummary (providers_added, providers_updated, prefs_applied) to model.rs. Create core/portable.rs: build_export(config_dir) reads the providers index (strip any key) + app prefs + account labels into ExportDoc; apply_import(config_dir, doc) validates the header (app==clavis) then merges — create/update providers KEYLESS (never write a key), apply prefs, never touch secrets/unknown keys -> ImportSummary; foreign/invalid header -> CoreError. Declare in core/mod.rs. commands/portable.rs: export_config(path) writes the JSON to the given path (atomic); import_config(path) -> ImportSummary; register in lib.rs. Rust tests: build_export output serialized contains the provider labels/baseUrls/prefs and NO key/token substring; apply_import merges keyless + applies prefs; a foreign header is rejected. | Restrictions: export NEVER serializes a key/token (assert in test); import never writes secrets; atomic. | Success: cargo test (portable green) + cargo build clean._

- [x] 2. core/backups.rs — rotating snapshots + restore + switch hook + Rust tests
  - Files: `src-tauri/src/core/backups.rs` (new), `src-tauri/src/core/mod.rs` (modify), `src-tauri/src/core/switch.rs` (modify), `src-tauri/src/core/providers.rs` (modify), `src-tauri/src/model.rs` (modify), `src-tauri/src/commands/backups.rs` (new), `src-tauri/src/commands/mod.rs` (modify), `src-tauri/src/lib.rs` (modify)
  - Purpose: a rotating, restorable backup store of the Claude files
  - _Leverage: src-tauri/src/core/(atomic_fs,claude_json,paths).rs_
  - _Requirements: 2.1, 2.2, 2.3, 2.4_
  - _Prompt: Implement the task for spec import-export-backups-latency, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Rust engineer | Task: Add BackupEntry (id, original, timestamp, size) to model.rs. Create core/backups.rs: snapshot(clavis_dir, claude_paths) copies each EXISTING file to clavis_dir/backups/NAME.TIMESTAMP.bak (atomic, 0600, dir 0700) then prune(name, keep=20) deletes oldest beyond 20; list(clavis_dir) -> Vec(BackupEntry) newest-first (missing dir -> empty); restore(clavis_dir, id) snapshots the CURRENT state first then atomically copies the backup back to its original path (preserve perms). Declare in core/mod.rs. Insert a snapshot(...) call at the START of core::switch::switch_account AND core::providers::apply (before mutating) so every switch auto-backs-up (resolve clavis dir + the 3 claude paths). commands/backups.rs: list_backups() / restore_backup(id) -> Result(_, CoreError); register in lib.rs. Rust tests: snapshot writes a timestamped copy + prune keeps newest 20; list newest-first; restore copies back AND snapshots current first; empty dir -> empty. | Restrictions: only Claude file CONTENT (never keyring); atomic; perms 0700/0600; rotation keeps newest N. | Success: cargo test (backups green) + cargo build clean._

- [x] 3. core/latency.rs — bounded endpoint latency + Rust test
  - Files: `src-tauri/src/core/latency.rs` (new), `src-tauri/src/core/mod.rs` (modify), `src-tauri/src/model.rs` (modify), `src-tauri/src/commands/latency.rs` (new), `src-tauri/src/commands/mod.rs` (modify), `src-tauri/src/lib.rs` (modify)
  - Purpose: measure provider endpoint round-trip without sending secrets
  - _Leverage: the existing reqwest HTTP client (S7 usage)_
  - _Requirements: 3.1, 3.2, 3.3_
  - _Prompt: Implement the task for spec import-export-backups-latency, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Rust engineer | Task: Add LatencyResult (ms Option(u64), ok bool, status Option(u16)) to model.rs. Create core/latency.rs: measure(base_url, samples=3, timeout=3s) does one warm-up then the median of `samples` timed GET/HEAD requests with NO auth header + the hard timeout; a reachable non-2xx still yields ms (ok true if a response arrived); timeout/connect error -> ok false, ms None. Declare in core/mod.rs. commands/latency.rs: test_latency(base_url) -> Result(LatencyResult, CoreError); register in lib.rs. Rust test: a reachable URL (e.g. a localhost test server or a known-good host with a short timeout) yields ms+ok; an unroutable address (e.g. 10.255.255.1) -> ok false within the timeout. | Restrictions: NO auth/key sent; hard timeout; never panic. | Success: cargo test (latency green) + cargo build clean._

- [x] 4. Frontend: Data & backups card + latency action + hooks
  - Files: `src/lib/ipc.ts` (modify), `src/lib/types.ts` (modify), `src/lib/queries.ts` (modify), `src/screens/settings/index.tsx` (modify), `src/screens/config-editor/index.tsx` (modify), `package.json` (modify), `src/i18n/locales/*.json` (modify)
  - Purpose: the UI for export/import/backups + per-provider latency
  - _Leverage: @/ui (Card, Button, Modal, Badge), @tauri-apps/plugin-dialog, src/lib/queries.ts_
  - _Requirements: 1.1, 1.3, 2.2, 2.3, 3.1_
  - _Prompt: Implement the task for spec import-export-backups-latency, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Frontend developer | Task: pnpm add @tauri-apps/plugin-dialog. Mirror ExportDoc summary types + BackupEntry + LatencyResult in types.ts; add to ipc.ts: exportConfig() (save dialog -> export_config), importConfig() (open dialog -> import_config), listBackups(), restoreBackup(id), testLatency(baseUrl). Add useBackups()/useImportConfig()/useRestoreBackup()/useExportConfig()/useTestLatency() to queries.ts (off-Tauri demo). Add a "Data & backups" Card to the Settings screen: Export button (save dialog + toast summary + a "no secrets are exported" note), Import button (open dialog + toast summary + invalidate providers/prefs), and a Backups list (useBackups: timestamp + size, newest-first) each with a confirm-guarded Restore (invalidate). In the config-editor, add a "Test latency" Button per provider -> useTestLatency showing ms or a timeout Badge. Add the i18n keys to en + the 4 locales. | Restrictions: components use hooks; demo fallback; tokens only; the export note must state secrets are excluded. | Success: tsc clean; the card + latency action work under Tauri._

- [x] 5. Tests (frontend)
  - Files: `src/screens/settings/DataBackups.test.tsx`, `src/lib/queries.test.ts` (modify)
  - Purpose: lock the new UI + hooks
  - _Leverage: Vitest + Testing Library, mocked ipc + dialog_
  - _Requirements: 1.1, 1.3, 2.3, 3.1_
  - _Prompt: Implement the task for spec import-export-backups-latency, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Frontend QA engineer | Task: DataBackups.test.tsx (ipc + dialog mocked): Export calls exportConfig; Import calls importConfig + toasts the summary; the Backups list renders mocked entries and Restore calls restoreBackup; (config-editor or a focused test) Test latency calls testLatency and shows ms. Extend queries.test.ts for useBackups + useTestLatency. | Restrictions: behavior not implementation; headless; mock backend + dialog. | Success: pnpm test green incl. new suites._

- [x] 6. Verify, fingerprint + no-secret-in-export audit
  - Files: (verify) whole repo
  - Purpose: prove S15 builds, tests pass, no fingerprints, and export carries no secret
  - _Leverage: tech.md de-fingerprint rules_
  - _Requirements: all_
  - _Prompt: Implement the task for spec import-export-backups-latency, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Release engineer | Task: Run pnpm exec tsc --noEmit (0), pnpm test (green), cd src-tauri && cargo test (portable+backups+latency green) + cargo build (clean), pnpm exec vite build (clean). Fingerprint grep over src + src-tauri/src + configs -> zero (incl. no predecessor file names). SECRET-SAFETY: confirm the portable export test asserts no key/token in the output, and that latency sends no auth header (read the code). Report exact pass/fail. (The orchestrator launches the window, screenshots the Data & backups card, commits.) | Restrictions: fix don't suppress; do not commit. | Success: all gates green; zero fingerprints; export proven secret-free._
