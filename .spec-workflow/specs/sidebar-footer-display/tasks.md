# Tasks Document — sidebar-footer-display (S18)

> Footer = display + navigate to Configurations (remove the inline switch Popover + "Sign in with Claude"); switching stays in Configurations + tray; the one add path is the existing honest AddAccountModal. Presentation-only — NO backend change; secrets/switch core untouched. Keep AccountAvatar/initialsOf exports + Configurations AccountRow switching intact. Tokens only. Keep all existing tests green (183 TS). Each task: set `[-]`, implement, `log-implementation`, then `[x]`.

- [x] 1. Footer → display + navigate; remove the switch Popover + dead code + store flags
  - Files: `src/app/AccountSwitcher.tsx` (modify), `src/lib/store.ts` (modify)
  - Purpose: turn the footer into a single-purpose nav card
  - _Leverage: store go(screen) action; existing card markup; @/ui/icons ChevronRight_
  - _Requirements: 1.1, 1.2, 1.3, 1.4, 3.1, 3.2, 3.3_
  - _Prompt: Implement the task for spec sidebar-footer-display, first run spec-workflow-guide to get the workflow guide then implement the task: Role: React engineer | Task: In src/app/AccountSwitcher.tsx, change the exported AccountSwitcher() (the sidebar footer) so it returns the existing card button DIRECTLY (no Popover wrapper) with onClick=(() => go("configs")) (from useShellStore), aria-label="View accounts in Configurations", dropping aria-haspopup/aria-expanded; replace the trailing ChevronsUpDown with a muted ChevronRight (var(--text-3)) as a navigation affordance. Keep reading activeIdentity for the label + the sub line, keep AccountAvatar/initialsOf. DELETE the now-dead popover: selectAccount, selectProvider, the Popover menu JSX, the local providerIsActive/RowText/PanelDivider, and every import/store-read that becomes unused (Popover, Check, ChevronsUpDown, Plus, useSwitchAccount, useApplyProvider, useAccounts, useProviders, accountIsActive, ProviderChip, brandForProvider, switcherOpen/openSwitcher/closeSwitcher, openAddAccount) — verify per symbol that it is truly unused before removing. Update the file doc comment to describe a display + navigate-to-Configurations card. In src/lib/store.ts remove switcherOpen + openSwitcher/closeSwitcher/toggleSwitcher (state + actions + their interface fields) ONLY if a repo-wide grep shows zero remaining consumers; leave addAccountOpen/openAddAccount/closeAddAccount + go intact. | Restrictions: presentation only; do NOT touch AccountAvatar/initialsOf exports, the AddAccountModal, the Configurations AccountRow, the tray, or any backend; tokens only; no dead code left. | Success: pnpm exec tsc --noEmit clean; the footer navigates to Configurations and opens no popover._

- [x] 2. Update tests + verify
  - Files: `src/app/Sidebar.test.tsx` (modify), any `src/app/AccountSwitcher*.test.tsx` (modify/remove), (verify) repo
  - Purpose: lock the new behavior + prove green
  - _Leverage: Vitest + Testing Library_
  - _Requirements: 1.2, 3.3_
  - _Prompt: Implement the task for spec sidebar-footer-display, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Frontend QA engineer | Task: Update the footer/sidebar tests: clicking the footer account card calls go("configs") (or sets activeScreen to "configs") and NO switch popover/menu (role="menu"/menuitemradio) appears; the active identity label still renders; AccountAvatar still shows. Remove or rewrite any test that asserted the old popover switch behavior (the Configurations rows + AddAccountModal tests already cover switching/adding and must stay green). VERIFY and report EXACT results: pnpm exec tsc --noEmit (0); pnpm test (green, file+test counts); pnpm exec vite build (clean); fingerprint grep over the touched files (ccmate|cc-switch|ccconfig|__ccmate__|posthog|phc_|59948|unlock_cc_ext) -> zero. Do NOT git commit (orchestrator screenshots + commits). | Restrictions: behavior not implementation; do not weaken unrelated tests; fix don't suppress. | Success: all gates green; footer-navigation asserted; no dead popover test remains._
