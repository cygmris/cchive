import { ScreenPlaceholder } from "@/app/ScreenHeader";

/** Usage screen (placeholder — real content lands in a later spec). */
export function UsageScreen() {
  return (
    <ScreenPlaceholder
      title="Usage"
      description="Token consumption across all your Claude Code sessions."
      label="Usage"
    />
  );
}
