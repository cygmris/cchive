import { ScreenPlaceholder } from "@/app/ScreenHeader";

/** Settings screen (placeholder — real content lands in a later spec). */
export function SettingsScreen() {
  return (
    <ScreenPlaceholder
      title="Settings"
      description="Clavis application preferences."
      label="Settings"
    />
  );
}
