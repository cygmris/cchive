import { ScreenPlaceholder } from "@/app/ScreenHeader";

/** Memory screen (placeholder — real content lands in a later spec). */
export function MemoryScreen() {
  return (
    <ScreenPlaceholder
      title="Memory"
      description="~/.claude/CLAUDE.md"
      label="Memory"
    />
  );
}
