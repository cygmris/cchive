import { ScreenPlaceholder } from "@/app/ScreenHeader";

/** Agents screen (placeholder — real content lands in a later spec). */
export function AgentsScreen() {
  return (
    <ScreenPlaceholder
      title="Agents"
      description="Sub-agents Claude Code can delegate specialized work to."
      label="Agents"
    />
  );
}
