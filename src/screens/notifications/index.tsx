import { ScreenPlaceholder } from "@/app/ScreenHeader";

/** Notifications screen (placeholder — real content lands in a later spec). */
export function NotificationsScreen() {
  return (
    <ScreenPlaceholder
      title="Notifications"
      description="Choose when Claude Code should ping you with a system notification."
      label="Notifications"
    />
  );
}
