import { ScreenPlaceholder } from "@/app/ScreenHeader";

/** Projects screen (placeholder — real content lands in a later spec). */
export function ProjectsScreen() {
  return (
    <ScreenPlaceholder
      title="Projects"
      description="Per-project configuration for every folder you've run Claude Code in."
      label="Projects"
    />
  );
}
