import { ScreenPlaceholder } from "@/app/ScreenHeader";

/** Commands screen (placeholder — real content lands in a later spec). */
export function CommandsScreen() {
  return (
    <ScreenPlaceholder
      title="Commands"
      description="Custom slash commands available in every Claude Code session."
      label="Commands"
    />
  );
}
