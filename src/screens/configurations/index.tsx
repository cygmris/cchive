import { ScreenPlaceholder } from "@/app/ScreenHeader";

/** Configurations screen (placeholder — real content lands in a later spec). */
export function ConfigurationsScreen() {
  return (
    <ScreenPlaceholder
      title="Configurations"
      description="Your keyring — Claude accounts and custom API providers in one place. Switch instantly."
      label="Configurations"
    />
  );
}
