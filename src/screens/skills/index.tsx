import { ScreenPlaceholder } from "@/app/ScreenHeader";

/** Skills screen (placeholder — real content lands in a later spec). */
export function SkillsScreen() {
  return (
    <ScreenPlaceholder
      title="Skills"
      description="Agent Skills Claude Code loads on demand — each a folder with a SKILL.md."
      label="Skills"
    />
  );
}
