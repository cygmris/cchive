import { ScreenPlaceholder } from "@/app/ScreenHeader";

/** Overview screen (placeholder — real content lands in a later spec). */
export function OverviewScreen() {
  return (
    <ScreenPlaceholder
      title="Overview"
      description="Your Claude Code control room — keys, servers and usage at a glance."
      label="Overview"
    />
  );
}
