import { ScreenPlaceholder } from "@/app/ScreenHeader";

/** MCP screen (placeholder — real content lands in a later spec). */
export function McpScreen() {
  return (
    <ScreenPlaceholder
      title="MCP"
      description="Global Model Context Protocol servers, available across every project."
      label="MCP"
    />
  );
}
